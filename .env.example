# frontend — SALIN ke .env lalu isi nilainya. JANGAN commit .env
# Semua panggilan API lewat API Gateway, tidak pernah langsung ke service backend.
VITE_API_BASE_URL=/api

# Dipakai vite dev server untuk mem-proxy /api saat pengembangan lokal
VITE_API_PROXY_TARGET=http://localhost:8080
